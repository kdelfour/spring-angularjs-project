CREATE TABLE RULE (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY, 
	TITLE VARCHAR(50) NOT NULL,
	DESCRIPTION VARCHAR(200),
	CODE VARCHAR(1024)
);

INSERT INTO RULE (TITLE,DESCRIPTION) VALUES (('R1','R1 DESC'),('R2','R2 DESC'), ('R3','R3 DESC'));